{
  "name": "fruit-drop",
  "version": "1.0.0",
  "description": "Southeast Asian Fruit Drop Game - Suika-style merge puzzle",
  "main": "index.html",
  "scripts": {
    "start": "npx serve .",
    "build": "rm -rf www && mkdir -p www/js www/css www/assets && cp index.html www/ && cp js/*.js www/js/ && cp css/*.css www/css/ && cp -r assets/* www/assets/ 2>/dev/null || true",
    "cap:sync": "npm run build && npx cap sync",
    "ss": "f=screenshots/$(date +%Y%m%d_%H%M%S).png && mkdir -p screenshots && ~/android-sdk/platform-tools/adb exec-out screencap -p > $f && echo $PWD/$f",
    "deploy": "npm run build && npx cap sync android && cd android && ANDROID_SDK_ROOT=~/android-sdk JAVA_HOME=$(/usr/libexec/java_home -v 21) ./gradlew assembleDebug && ~/android-sdk/platform-tools/adb install -r app/build/outputs/apk/debug/app-debug.apk"
  },
  "keywords": [
    "game",
    "fruit",
    "merge",
    "puzzle",
    "suika"
  ],
  "license": "MIT",
  "dependencies": {
    "@capacitor-community/admob": "^8.0.0",
    "@capacitor/android": "^8.1.0",
    "@capacitor/cli": "^8.1.0",
    "@capacitor/core": "^8.1.0"
  }
}
